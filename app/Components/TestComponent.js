var React = require('react');

var TestUnit = React.createClass({

  render: function () {
    return(
       <div className="container default">
         <a href = "/another"> let's go to maps </a>
       </div>
     );
    }
});

module.exports = TestUnit;
