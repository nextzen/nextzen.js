machine:
  node:
    version: 0.10.33

dependencies:
  override:
    - npm install
    - npm install bower -g
    - npm install webpack -g
    - npm install webpack

test:
  override:
    - echo 'perfection requires no tests'

deployment:
  staging:
    branch: master
    commands:
      - ./deploy dev
  production:
    branch: production
    commands:
      - ./deploy production
