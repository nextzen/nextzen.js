machine:
  node:
    version: 4.2.2
general:
  artifacts:
    - "dist"

deployment:
  production:
    branch: baldur/*
    commands:
      - npm install
      - npm run build
      - aws s3 sync dist "s3://erasermap.com/" --exclude "what_to_exclude*"
