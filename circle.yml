machine:
  node:
    version: 0.10.33

dependencies:
  override:
    - npm install
    - npm install bower -g
    - npm install webpack -g

deployment:
  staging:
    branch: master
    commands:
      - ./deploy dev
  production:
    branch: production
    commands:
      - ./deploy production
