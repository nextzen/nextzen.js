machine:
  node:
    version: 0.10.33

dependencies:
  override:
    - npm install
    - sudo npm install bower -g

deployment:
  staging:
    branch: master
    commands:
      - ./deploy dev
  production:
    branch: production
    commands:
      - ./deploy production
